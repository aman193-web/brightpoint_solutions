# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/

# Vite / tooling caches
.vite/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment — never commit secrets
.env
.env.*
!.env.example

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Brand deliverables
# ------------------
# The guidelines PDF, the asset zip and the generator output are a client
# hand-off, not source. They are ~11 MB of binaries between the two folders
# (Deliverables/ mirrors brand/), so they are delivered separately and kept
# out of the repo. brand/src/ regenerates all of it if it is ever needed.
brand/
Deliverables/
